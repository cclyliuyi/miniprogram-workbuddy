# 云函数 getPhotos 部署指南（根治编译 timeout）

本次已把数据查询从「客户端直连云数据库」改为「**云函数服务端查询**」，冷启动超时问题可根治。但**云函数需要你在本机开发者工具里手动上传一次**（AI 无法替你部署）。

## 步骤（2 分钟）

1. 打开**微信开发者工具**，导入/打开本项目（`miniprogram/` 目录）。
2. 左侧文件树找到 `cloudfunctions/getPhotos` 文件夹。
3. **右键**它 → 选择 **「上传并部署：云端安装依赖」**。
   - 注意选「云端安装依赖」，不用本地装 node_modules。
4. 等 10~30 秒，控制台提示「上传成功」即可。
5. **重新编译**小程序。

## 不部署会怎样？

`utils/db.js` 已做**自动降级**：检测到云函数不可用时会退回客户端直查（带重试）。
- 部署后：首查走云函数，快且稳，编译不再报 `Error: timeout`。
- 未部署：功能仍可用，但首查可能仍慢（旧行为），控制台会打印一行 `[db] 云函数不可用，降级客户端直查`，属正常提示，不是报错。

## 验证是否生效

编译后打开任意月份，控制台应输出：
```
[db] getMonthPhotos (cache) 7 31   ← 命中缓存，秒开
[db] warmAll 完成，月份数: 12
```
且**不再出现 `Error: timeout`** 即为成功。

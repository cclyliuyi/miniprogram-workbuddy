# 天线与电磁波知识日历 · 发布与预览指南

> 当前小程序 appid：`wxd9967fc9c09f3b40`
> 云开发环境：`cloud1-d3gsxamaw26beccb8`
> 项目目录：`D:\WorkBuddy\03_images_final\miniprogram`

---

## 三种方式一览

| 方式 | 需要审核？ | 谁能看 | 适合场景 |
|---|---|---|---|
| **真机预览** | 否 | 扫码的你自己 | **最快在手机看效果（推荐）** |
| **体验版** | 否 | 你 + 体验成员 | 长期测试 / 分享给同事朋友 |
| **正式发布** | 是（1–7 天） | 全部微信用户 | 上线运营 |

---

## 一、真机预览（最快看效果，推荐你用这个）

前置：你本机装了「微信开发者工具」，且登录的是小程序管理员或开发者微信号。

1. 打开微信开发者工具 → **导入项目** → 目录选 `D:\WorkBuddy\03_images_final\miniprogram`
   （appid 已自动填好 `wxd9967fc9c09f3b40`，无需手填）
2. 工具自动编译完成后，点工具栏右侧的 **「预览」** 按钮（或快捷键 `Ctrl+Shift+P`）
3. 工具生成一个二维码 → 用**手机微信扫这个码** → 自动在真机打开小程序
4. 现在就能在手机上浏览：日历（双历缩略图）、年视图、日详情（前后翻+左右滑）、故事流、生成日签卡

> 预览码有效期较短（约半小时），过期重新点「预览」即可。

---

## 二、体验版（长期分享 / 给朋友测）

1. 开发者工具点 **「上传」** → 填版本号（如 `1.0.0`）和备注 → 上传
2. 浏览器登录 <https://mp.weixin.qq.com> → **管理** → **版本管理** → 体验版 → 选刚上传的版本 **「设为体验版」**
3. **成员管理** → 体验成员 → 添加同事 / 朋友的微信号
4. 他们扫体验版二维码即可长期使用

---

## 三、正式发布（上线全网）

1. 同上「上传」代码
2. mp 后台 → **版本管理** → 开发版本 → **提交审核**
3. 填写：
   - **类目**：建议选「工具 - 日历」或「图片/摄影」
   - **名称**：天线与电磁波知识日历
   - **功能页截图**、**隐私协议**（微信强制要求）
4. 等待审核（通常 1–7 天）
5. 审核通过 → 点 **「发布」** → 全网可见

---

## ⚠️ 真机必查：否则手机上图片全白（90% 的真机白屏都栽在这）

模拟器因为有本地缓存可能看图正常，但**真机一定走云端**，这两项权限必须开：

1. **云数据库权限**
   云开发控制台 → 数据库 → `calendar_photos` 集合 → 权限设置 →
   改为 **「所有用户可读，仅创建者可读写」**

2. **云存储权限**
   云开发控制台 → 存储 → 权限设置 →
   改为 **「所有用户可读」**

> 之前开发过程中如果没开这两项，现在不开，手机上日历缩略图、日详情大图、故事流、日签卡都会是空白或加载失败。

---

## 常见问题

- **提示"你不是该小程序的开发者"**：让管理员在 mp 后台把你加为「开发者」或「体验成员」。
- **预览能看，体验版/正式版白屏**：还是上面两个权限问题，或云环境被删；确认 `cloud1-d3gsxamaw26beccb8` 环境仍存在。
- **切月偶发卡顿**：首次进入会后台预热 365 天缓存，预热完成后切月秒开；若仍频繁 timeout，建议把数据查询迁到云函数（服务端 limit 上限 100，更稳）。
- **想长期自己看**：用「体验版」比反复扫预览码更省事。

# 天线与电波传播知识日历 — 改进建议清单

> 调研来源：Things 3 / Apple Calendar / Fantastical / Cron / Duolingo Streak / Co-Star / Bloom / Whering / Instagram Stories Archive 等 30+ 全球顶尖应用，结合 Mobbin / Muzli / Eleken 设计案例库。
> 对照代码：逐一审查了 calendar、day-detail、story、year、favs、card 全部页面 + utils 层。

---

## 总览：按优先级排序

| 优先级 | 改进项 | 类型 | 工作量 |
|--------|--------|------|--------|
| 🔴 P0 | 1. 触觉反馈（振动反馈） | 微交互 | 小 |
| 🔴 P0 | 2. 月历「今天」高亮 + 回到今天 | 核心交互 | 小 |
| 🔴 P0 | 3. 搜索 / 跳转任意天 | 功能补全 | 中 |
| 🟡 P1 | 4. 连续打卡里程碑奖励 | 游戏化 | 中 |
| 🟡 P1 | 5. 知识卡片标注「重要程度」视觉区分 | 信息层级 | 小 |
| 🟡 P1 | 6. 日签卡增加知识标签 + 主题色块 | 分享优化 | 小 |
| 🟡 P1 | 7. 故事流日期指示器（进度条） | 导航增强 | 小 |
| 🟡 P1 | 8. 年视图缩略图手势预览（长按放大） | 交互优化 | 中 |
| 🟢 P2 | 9. 骨架屏统一化（全页面 loading 一致性） | 视觉打磨 | 小 |
| 🟢 P2 | 10. 收藏页排序 + 筛选 | 功能完善 | 中 |
| 🟢 P2 | 11. 知识卡片「已学概念」术语表页 | 知识管理 | 大 |
| 🟢 P2 | 12. 月历格子增加农历完整显示 | 本土化 | 小 |
| 🟢 P2 | 13. 暗色模式全页面审查 | 视觉打磨 | 中 |
| 🟢 P2 | 14. 无障碍 / 大字模式适配 | 可访问性 | 中 |
| 🟢 P2 | 15. 引导页 / 首次使用 onboarding | 用户教育 | 中 |

---

## P0 — 立即可做，效果立竿见影

### 1. 🔴 触觉反馈（Haptic Feedback）

**问题**：当前所有交互（翻卡、收藏、切换日期、保存日签卡）都没有振动反馈，用户点完不确定操作是否生效。

**参考**：
- Apple Reminders：完成时有轻振动 + 对勾动画
- Duolingo：答对有 medium 振动，答错有 warning 振动
- 微信原生：收藏时有 light 振动

**建议**：
```javascript
// utils/haptic.js（新建）
function light() { wx.vibrateShort({ type: 'light' }) }
function medium() { wx.vibrateShort({ type: 'medium' }) }
function heavy() { wx.vibrateShort({ type: 'heavy' }) }

module.exports = { light, medium, heavy }
```

**接入点**：
| 场景 | 强度 | 代码位置 |
|------|------|----------|
| 翻卡 onTap | light | `day-detail.js → onStageTap` |
| 收藏 toggleFav | medium | `day-detail.js → toggleFav` |
| 首次打卡 justChecked | heavy | `day-detail.js → updateCurrent` |
| 月份切换 changeMonth | light | `calendar.js → changeMonth` |
| 保存日签卡成功 | medium | `card.js → _saveToAlbum success` |
| swiper 滑动换天 | （不加，太频繁） | — |

**注意**：微信小程序只有 `vibrateShort({ type })`，type 仅支持 `light/medium/heavy`（基础库 2.13.0+）。振动频率不宜过高——每次滑动都振会让用户厌烦。

---

### 2. 🔴 月历「今天」高亮 + 快速回到今天

**问题**：月历网格中，当前月今天虽有 `is-today` class，但视觉上不够突出。另外浏览到其他月份后，没有一键回到今天的入口。

**参考**：
- Apple Calendar：今天是实心圆 + 高亮色填充
- Things 3：今天有圆点标记
- Google Calendar：今天用主题色圆圈包裹

**建议**：

**(A) 强化今天的高亮样式**：
```css
/* calendar.wxss */
.cell-card.is-today {
  position: relative;
}
.cell-card.is-today::before {
  content: '';
  position: absolute;
  top: 4rpx; right: 4rpx;
  width: 14rpx; height: 14rpx;
  border-radius: 50%;
  background: var(--accent);
  box-shadow: 0 0 0 4rpx rgba(176, 106, 79, 0.25);
  z-index: 5;
}
/* 日期数字用赤陶色 + 加粗 */
.cell-card.is-today .date-solar {
  color: var(--accent);
  font-weight: 700;
}
```

**(B) 顶部导航增加「回到今天」按钮**：
```xml
<!-- calendar.wxml → float-top -->
<view class="ft-nav">
  <view class="ft-btn" bindtap="goToday" wx:if="{{month !== todayMonth}}">今</view>
  <view class="ft-btn" bindtap="changeMonth" data-delta="-1">‹</view>
  <view class="ft-btn" bindtap="changeMonth" data-delta="1">›</view>
</view>
```

```javascript
// calendar.js
goToday() {
  const now = new Date();
  const month = now.getMonth() + 1;
  if (month === this.data.month) return;
  this.setData({ month, monthLabel: `${month}月`, monthThumb: '', loading: true });
  this.loadMonth(month);
  this.loadMonthThumb(month);
}
```

---

### 3. 🔴 搜索 / 跳转任意天

**问题**：如果用户想直接跳到某个特定知识点（比如"想复习微带天线"），只能从月历一天天翻，效率很低。

**参考**：
- Fantastical：自然语言搜索
- Notion：搜索栏 + 标签筛选

**建议**：新增搜索页，从 calendar 顶部加搜索图标入口。

**方案**：
```xml
<!-- calendar.wxml → float-top 增加搜索按钮 -->
<view class="ft-btn ft-search" bindtap="goSearch">🔍</view>
```

```javascript
// pages/search/search.js（新建）
const { CARDS } = require('../../utils/quotes');

Page({
  data: { keyword: '', results: [] },

  onInput(e) {
    const kw = e.detail.value.trim();
    if (!kw) { this.setData({ results: [] }); return; }
    const results = CARDS.filter(c =>
      c.topic.includes(kw) ||
      c.hook.includes(kw) ||
      c.body.includes(kw) ||
      (c.tag && c.tag.includes(kw))
    ).map(c => {
      const m = parseInt(c.date.match(/(\d+)月/)[1]);
      const d = parseInt(c.date.match(/(\d+)日/)[1]);
      return { ...c, month: m, day: d };
    });
    this.setData({ results });
  },

  openResult(e) {
    const { month, day } = e.currentTarget.dataset;
    wx.navigateTo({
      url: `/pages/day-detail/day-detail?month=${month}&day=${day}`,
    });
  },
});
```

**搜索页 UI**：
- 顶部搜索框（自动 focus）
- 结果列表：每条显示 hook + tag + date
- 点击跳转到 day-detail

---

## P1 — 值得做，显著提升体验

### 4. 🟡 连续打卡里程碑奖励

**问题**：当前的 streak 只是一个数字，缺乏「达成感」。Duolingo 的研究显示，里程碑奖励能让 7 日留存提升 15-30%。

**参考**：
- Duolingo：3 天/7 天/30 天/100 天里程碑，发光的日历格子 + 特殊动画
- Habitica：连续天数达标的徽章奖励
- Flo：花卉图案装饰打卡日

**建议**：

**(A) 里程碑弹窗**：当连续打卡达到 3/7/14/30/50/100 天时，全屏弹出庆祝动画。

```javascript
// utils/progress.js 增加
const MILESTONES = [3, 7, 14, 21, 30, 50, 100, 200, 365];

function checkMilestone(streakCount) {
  return MILESTONES.includes(streakCount);
}

function getMilestoneTitle(count) {
  const titles = {
    3: '入门三日', 7: '一周坚持', 14: '半月有成',
    21: '习惯养成', 30: '月度达人', 50: '半百之旅',
    100: '百日筑基', 200: '双百精进', 365: '全年通关',
  };
  return titles[count] || '';
}
```

```xml
<!-- day-detail.wxml 增加里程碑弹窗 -->
<view class="milestone-overlay" wx:if="{{showMilestone}}" bindtap="closeMilestone">
  <view class="milestone-card pop">
    <view class="ms-badge num">{{milestoneCount}}</view>
    <view class="ms-title display">{{milestoneTitle}}</view>
    <view class="ms-desc">连续打卡 {{milestoneCount}} 天</view>
    <view class="ms-flame">🎉</view>
    <button class="btn grad" bindtap="closeMilestone">继续学习</button>
  </view>
</view>
```

**(B) 日历格子打卡可视化**：已读的天不只是小圆点，而是用一个柔和的绿色填充（参考 Duolingo 的发光格子），让用户一眼看到自己的"学习地图"。

```css
/* 已读格子：柔和的绿色叠加 */
.cell-card.is-read::after {
  content: '';
  position: absolute;
  inset: 0;
  background: rgba(122, 145, 129, 0.12);
  border-radius: inherit;
  pointer-events: none;
}
```

---

### 5. 🟡 知识卡片「重要程度」视觉区分

**问题**：quotes.js 中每条卡片都有 `level` 字段（重要/一般/不太重要），但当前 UI 完全没有使用这个信息，所有卡片视觉权重一样。

**参考**：
- Things 3：重要任务加星标
- Notion：高优先级条目用色块标注

**建议**：在知识盲盒区域增加重要程度的视觉标记。

```xml
<!-- day-detail.wxml → kn-head 区域 -->
<view class="kn-head">
  <view class="kn-tag-group">
    <view class="kn-tag num" wx:if="{{card.tag}}">{{card.tag}}</view>
    <view class="kn-level kn-level-{{card.levelType}}" wx:if="{{card.levelType}}">
      {{card.levelType === 'important' ? '★ 核心' : ''}}
      {{card.levelType === 'general' ? '· 一般' : ''}}
    </view>
  </view>
  <view class="kn-idx num">DAY {{cardIdx}}</view>
</view>
```

```css
.kn-level-important {
  color: var(--accent);
  font-size: 20rpx;
  font-weight: 600;
}
.kn-level-general {
  color: var(--muted);
  font-size: 20rpx;
}
```

在 `pickCard` 或 `onLoad` 中将中文 level 转成英文 key：
```javascript
function levelKey(level) {
  if (level === '重要') return 'important';
  if (level === '一般') return 'general';
  return 'minor';
}
```

---

### 6. 🟡 日签卡增加知识标签 + 主题色块

**问题**：日签卡目前只有图片 + 一句话金句 + 品牌水印，信息密度较低。对比 Co-Star（每日占星卡）每张卡片都有日期 + 标签 + 主题色，视觉更丰富。

**建议**：在日签卡 canvas 绘制中增加：

**(A) 顶部知识标签**（图片上方）：
```javascript
// card.js → drawCard() 中，在画图片之前先画标签
ctx.fillStyle = theme.accent;
ctx.font = '600 11px sans-serif';
ctx.textAlign = 'left';
ctx.fillText(cardData.tag || '电磁波', padX, 20);
```

**(B) 知识点 hook 作为副标题**（金句上方）：
```javascript
// 在装饰竖线和金句之间，加一行 hook 标题
ctx.fillStyle = theme.ink;
ctx.font = 'bold 14px sans-serif';
ctx.fillText(cardData.hook.substring(0, 18), padX + 12, textTop);
// 然后是原有的金句 body
```

---

### 7. 🟡 故事流日期指示器（进度条）

**问题**：故事流（story 页面）滑动时，用户不知道当前是第几天/总多少天，缺乏「阅读进度」的感知。

**参考**：
- Instagram Stories：顶部的分段进度条
- 抖音 / 小红书：底部进度指示

**建议**：在故事流顶部加一组细进度条（Instagram Stories 风格）。

```xml
<!-- story.wxml → st-top 下方 -->
<view class="st-progress" wx:if="{{list.length > 1}}">
  <view 
    wx:for="{{list}}" wx:key="day"
    class="st-progress-bar {{index < current ? 'done' : ''}} {{index === current ? 'active' : ''}}">
  </view>
</view>
```

```css
.st-progress {
  display: flex;
  gap: 4rpx;
  padding: 0 30rpx 10rpx;
}
.st-progress-bar {
  flex: 1;
  height: 4rpx;
  background: rgba(32, 32, 28, 0.12);
  border-radius: 2rpx;
  transition: background 0.3s;
}
.st-progress-bar.done {
  background: var(--accent);
}
.st-progress-bar.active {
  background: var(--accent);
  opacity: 0.5;
}
```

---

### 8. 🟡 年视图长按预览大图

**问题**：年视图 12 格缩略图太小，点击直接跳转到月历。用户如果想快速预览某个月的大图，没有中间状态。

**参考**：
- Instagram：长按 Feed 预览大图（3D Touch / Peek）
- Pinterest：长按弹出详情卡片

**建议**：长按年视图格子，弹出半屏大图预览浮层（不跳转页面）。

```javascript
// year.js
onLongPress(e) {
  const month = e.currentTarget.dataset.month;
  const item = this.data.months.find(m => m.month === month);
  if (!item || !item.hasPhoto) return;
  this.setData({ previewMonth: item });
},

closePreview() {
  this.setData({ previewMonth: null });
},
```

```xml
<!-- year.wxml 增加预览浮层 -->
<view class="yr-preview" wx:if="{{previewMonth}}" bindtap="closePreview">
  <image class="yr-preview-img" src="{{previewMonth.thumb}}" mode="widthFix" />
  <view class="yr-preview-label num">{{previewMonth.label}}</view>
</view>
```

---

## P2 — 锦上添花，有空再做

### 9. 🟢 骨架屏统一化

**问题**：calendar 页有骨架屏 shimmer 效果，但 favs 页只有「加载中…」文字，story 页无加载状态。视觉一致性不够。

**建议**：将 `app.wxss` 中的 `.skeleton` shimmer 样式推广到所有加载场景。

**favs 页骨架屏**：
```xml
<view wx:if="{{loading}}" class="fv-list">
  <view wx:for="{{[0,1,2,3]}}" wx:key="*this" class="fv-card">
    <view class="skeleton" style="width:220rpx;height:200rpx;border-radius:12rpx;"></view>
    <view class="fv-info" style="flex:1;">
      <view class="skeleton" style="width:120rpx;height:32rpx;border-radius:8rpx;"></view>
      <view class="skeleton" style="width:100%;height:48rpx;margin-top:12rpx;border-radius:8rpx;"></view>
    </view>
  </view>
</view>
```

---

### 10. 🟢 收藏页排序 + 筛选

**问题**：收藏列表目前只能按收藏时间倒序排列，无法按月份、标签、重要程度筛选。

**参考**：
- Things 3：按项目/区域/标签分层
- Notion：多维度筛选

**建议**：
- 顶部增加排序切换：按时间 / 按月份 / 按标签
- 月份快速跳转条（1-12 月横向滚动 chip）

---

### 11. 🟢 知识术语表页

**问题**：365 天的知识点散落在各天，用户无法系统性回顾"我已经学了哪些概念"。

**参考**：
- Apple Books：阅读笔记 / 高亮列表
- Notion：知识库目录

**建议**：新增「术语表」页面，把 365 条知识按主题分组（天线类型/传播原理/频段/测量方法等），每条可点击跳转到对应天。

---

### 12. 🟢 月历农历完整显示

**问题**：当前只显示农历日（如"初一""十五"），没有显示节气和节日。

**建议**：集成轻量农历库，在格子底部小字显示节气/节日（如"立春""中秋"），增强文化氛围。

---

### 13. 🟢 暗色模式全页面审查

**问题**：app.wxss 有暗色模式变量定义，但各页面 wxss 中有大量硬编码颜色值（如 `rgba(176, 106, 79, .08)`），暗色模式下可能对比度不足。

**建议**：系统审查所有页面，将硬编码的 rgba 值替换为暗色模式自适应变量。

---

### 14. 🟢 无障碍 / 大字模式

**问题**：当前没有适配系统「大字模式」，老年人或视力不好的用户体验差。

**参考**：Apple Accessibility、微信无障碍指南

**建议**：
- 关键文字加 `aria-label`
- 确保所有可点击区域 ≥ 44pt
- 适配 `wx.getSystemInfoSync().fontSizeSetting`

---

### 15. 🟢 首次使用引导页

**问题**：新用户打开后不知道怎么操作（翻卡、收藏、生成日签卡等功能需要发现）。

**参考**：Duolingo、小红书的 onboarding 流程

**建议**：首次进入时，在日历页覆盖半透明引导层，高亮关键操作区域。

---

## 设计灵感速查表

| 设计元素 | 参考应用 | 可借鉴的点 |
|----------|----------|------------|
| 极简日历 | Things 3 | 柔和渐变标记当前日期，移除视觉杂乱 |
| 自然语言 | Fantastical | 日期搜索/跳转的自然交互 |
| 发光格子 | Duolingo | 打卡日的视觉强化（游戏化激励） |
| 每日符号 | Co-Star | 每天一个标志性符号暗示主题 |
| 视觉穿搭卡 | Whering | 用图片替代文字的日期展示 |
| 故事归档 | Instagram | 日历作为回顾过去的工具 |
| 进度分段条 | Instagram Stories | 阅读进度可视化 |
| 花卉装饰 | Bloom | 打卡日的装饰元素 |
| 长按预览 | Instagram/Pinterest | 快速预览不跳转 |
| 微交互反馈 | Apple Reminders | 完成操作的触觉+视觉确认 |

---

## 技术债务提醒

在开发新功能前，以下技术债建议先清理：

1. **tabBar 图标缺失**：当前 tabBar 没有图标（iconPath/selectedIconPath），微信审核时可能被要求补充
2. **分享标题可优化**：`onShareAppMessage` 的 title 目前是"X月X日 的照片"，可改为更有吸引力的知识 hook
3. **story 页面的 `btn accent`**：story 页日签卡按钮仍用 `.btn.accent`（黑底），和 day-detail 已改为 `.btn.grad`（渐变）不一致
4. **性能监控**：没有接入微信性能监控（wx.reportPerformance），线上卡顿无法感知

---

*本报告基于对 30+ 全球顶尖日历应用和设计案例的系统调研，结合当前项目代码的逐一审查生成。建议按优先级逐步实施，P0 项可立即动手，效果立竿见影。*

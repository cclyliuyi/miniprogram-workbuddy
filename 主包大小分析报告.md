# 主包大小分析报告

> 微信小程序主包限制：**2 MB**（2048 KB）。超过无法预览/上传。

## 一、当前状态

| 指标 | 数值 | 状态 |
|------|------|------|
| **主包源文件** | **2,418 KB (2.36 MB)** | 超限 |
| 微信压缩后估计 | ~1,572 KB (1.53 MB) | 安全但接近 |
| 限制 | 2,048 KB | — |

> 源文件 2.36 MB 超过 2 MB 限制，但微信开发者工具会对 JS/WXSS/WXML 做代码压缩，**实际打包约为源文件的 60%-70%**。所以目前**大概率在限制内**，但余量不多。

## 二、主包构成明细

| 组件 | 源文件大小 | 说明 |
|------|-----------|------|
| app 全局 (app.js/json/wxss) | 11 KB | 极小 |
| calendar 页 | 37 KB | 含日历逻辑 |
| favs 页 | 10 KB | 极小 |
| **eit 页** | **742 KB** | 784KB 图片 + 数据 |
| **method 页** | **905 KB** | 928KB 图片 + 数据 |
| tools/index | 24 KB | 工具页入口 |
| **utils/** | **687 KB** | 含 quotes 数据 |
| cloudfunctions/ | 1 KB | 极小 |
| **总计** | **2,418 KB** | — |

## 三、三大占用大户

### 1. method 页：905 KB（37%）
- `pages/method/images/`：**928 KB**（13 张图片）
- `pages/method/method-data.js`：20 KB
- 图片是绝对主力

### 2. eit 页：742 KB（31%）
- `pages/eit/images/`：**784 KB**（13 张图片）
- `pages/eit/eit-data.js`：12 KB
- 同样是图片为主

### 3. utils/：687 KB（28%）
- `utils/quotes/` 目录：248 KB（12 个月份分片 m01~m12 + index.js）
- `utils/quotes.js`：**220 KB**（旧版合并文件，与 quotes/ 重复！）
- `utils/quotes_old_bak.js`：**217 KB**（备份文件）
- 其他工具函数：~2 KB

## 四、立即可做的优化

### ⚡ 清理冗余文件（省 437 KB，零风险）

| 文件 | 大小 | 操作 |
|------|------|------|
| `utils/quotes.js` | 220 KB | 删除（已被 `quotes/` 目录替代） |
| `utils/quotes_old_bak.js` | 217 KB | 删除（纯备份） |

**验证**：代码中 `require('../../utils/quotes')` 会解析到 `utils/quotes/index.js`（目录入口），不依赖 `quotes.js`。两者内容完全重复。

**优化后**：
- 源文件：2,418 → **1,981 KB**（1.93 MB）
- 压缩估计：1,572 → **1,288 KB**（1.26 MB）
- 余量：约 **760 KB** 可用空间

### 📦 进一步优化（如需更多空间）

| 方案 | 节省 | 难度 |
|------|------|------|
| eit/method 图片转 WebP | ~400 KB | 低 |
| eit/method 图片上云存储（云函数加载） | ~1.7 MB | 中 |
| quotes/ 数据移入云数据库按月加载 | 248 KB | 中 |

## 五、分包情况

- **主包**：5 个 TabBar 页面 + utils + 图片
- **分包**：30 个（day-detail, card, eit-detail, tool-calc, iv-friis, iv-3dlab 等）
- 3D 模块的 threejs-miniprogram（597 KB）已在 `iv-3dlab` 分包，不占主包空间

主包 + 全部分包合计可达 20 MB（微信限制），当前远未触及。

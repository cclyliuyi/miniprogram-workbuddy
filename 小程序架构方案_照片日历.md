# 微信小程序架构设计方案：「每日一图」照片日历

> 目标：把项目文件夹里的 730 张日期命名照片（全年 365 天 × 正面/反面），转成一款可快速上线、易获客的微信小程序。
> 文档日期：2026-07-06（巧了，今天正是 7月6日 —— 可以做"历史上的今天"钩子）

---

## 一、资产现状（已实测）

| 指标 | 数值 | 对架构的影响 |
|---|---|---|
| 文件总数 | 730 张 PNG | — |
| 日期覆盖 | 1–12 月全年 365 天 | 天然就是一个"整年日历" |
| 结构 | 每天 `正面`+`反面` 各 1 张 | 适合"翻卡"交互 |
| 总体积 | **1.8 GB** | **绝不能进包，必须上云** |
| 单张大小 | ~3 MB | 需生成缩略图/预览图才可用 |
| 命名 | `X月X日正面.png` | 文件名即日历坐标，可自动解析 |

---

## 二、核心约束与关键决策

### 2.1 包大小铁律
- 主包 2MB、整包（含分包）20MB。**1.8GB 图片必须走云存储/CDN，运行时按 URL 拉取。**
- 小程序包内只放：代码、配置、占位图、少量图标。图片 0 张进包。

### 2.2 存储 / 后端方案（推荐：微信云开发）
| 方案 | 上线速度 | 运维 | 适合度 |
|---|---|---|---|
| **微信云开发 CloudBase（推荐）** | ⚡最快，无服务器 | 几乎零 | ★★★★★ |
| 自有服务器 + 对象存储 CDN | 慢，需备案/部署 | 高 | ★★★ |
| 纯静态托管（COS+OSS+CDN） | 中 | 中 | ★★★★ |

**结论**：要"快速上线"，首选**云开发**——云存储放图、云数据库存映射、云函数做可选的后台统计/分享卡合成，全部免服务器。

### 2.3 图片必须预处理（否则必卡）
原图 3MB 直接进网格会拖垮首屏。需三档：
- **缩略图 thumb**：~400px，WebP，<60KB → 月历网格用
- **预览图 preview**：~1080px，WebP，<200KB → 翻卡/大图用
- **原图 original**：保留，仅"查看大图"时按需加载

---

## 三、图片 → 日历 的转换管线（数据怎么转）

这是"把图片变成日历"的核心，用一条**本地预处理脚本** + **云开发**完成：

```
① 解析文件名         "7月6日正面.png"  →  { month:7, day:6, side:'front' }
② 生成三档图         original / preview(1080) / thumb(400)  全部转 WebP
③ 上传云存储         拿到 fileID / HTTPS 临时或 CDN URL
④ 写入云数据库       集合 calendar_photos：
                     { _id, month, day, front:{fileId,thumb,preview,original},
                       back:{fileId,thumb,preview,original}, caption? }
⑤ 小程序按月份查询    WHERE month = 当前月  →  渲染网格
```

**为什么这样转最好**：文件名已经自带"日期"语义，无需人工排布；正反面对应同一天，天然形成"一张卡的正反两面"。转换是**确定性、可批量、可重跑**的，365 天一次跑完。

---

## 四、小程序架构（目录与页面）

```
miniprogram/
├── app.js / app.json / app.wxss          # 全局配置、tabBar、分包声明
├── project.config.json / sitemap.json
├── pages/
│   ├── calendar/      # 【主页 tabBar】月历网格（7列，每天格显示正面缩略图）
│   ├── day-detail/    # 日详情·翻卡（正面↔反面，点击翻转 + 大图预览）
│   ├── year/          # 年视图（12个月缩略拼图，跳月）
│   └── story/         # 故事流（全屏左右滑浏览某月序列）
├── subpackages/
│   └── mine/          # 【我的】分包：关于、缓存清理、隐私协议
├── components/
│   ├── calendar-grid/ # 月历网格（含空白日占位、今天高亮）
│   ├── day-cell/      # 单日格（缩略图 + 是否有反面标记）
│   ├── photo-flip/    # 正反面翻卡（CSS 3D 翻转，Skyline 下更顺滑）
│   └── share-card/    # canvas 合成"日签"分享卡
├── utils/
│   ├── request.js     # 云开发/HTTPS 统一请求包装（Promisify）
│   ├── auth.js        # wx.login + 云开发匿名/微信登录
│   └── date.js        # 文件名↔日期解析、农历/节日扩展位
└── cloudfunctions/
    ├── importPhotos/  # （可选）服务端批量入库
    └── shareStats/    # （可选）分享/日活统计
```

**分包策略**：主页 `calendar` + 详情 `day-detail` 进主包（用户首屏必经）；`year`/`story`/`mine` 放分包，控制主包 < 1.5MB。

---

## 五、日历展示方案（怎么展示更好）—— 推荐组合

| 形态 | 说明 | 角色 | 推荐度 |
|---|---|---|---|
| **A. 月历网格** | 7列标准日历，每天格显示**正面缩略图**，无图日留白；今天高亮 | **主页（核心）** | ★★★★★ |
| **B. 翻卡详情** | 点某天进入，正面图；**点击翻转**露出反面（像明信片/相片卡） | **核心互动** | ★★★★★ |
| **C. 故事流滑动** | 全屏左右滑，按日期顺序看某月全部照片 | 沉浸浏览 | ★★★★ |
| **D. 年视图** | 12 个月缩略拼图，一眼"照片地图"，快速跳月 | 全局导航 | ★★★★ |

**为什么这样展示最好**（对比普通相册画廊）：
1. **显式日期语义**：用户打开就看到"哪天有照片、今天对应哪张"，符合"日历"心智，比滚动相册更有记忆点。
2. **正反面对应翻卡**：你的资产天生是"正+反"，翻卡交互把这点变成产品特色，而非普通九宫格。
3. **首屏即"全年地图"**：月历网格一屏尽览当月，分享欲强（"看我 7月6日 拍的"）。
4. **多形态覆盖不同场景**：网格=总览、翻卡=细看、故事流=漫刷、年视图=跳年。

> 若反面是**文字笔记/日记**而非另一张照片，翻卡背面就渲染为"文字卡片 + 正面小图"；若是**另一张照片**则双图翻转。这点需你确认（见第九节）。

---

## 六、增长与留存设计（快速获客）

| 手段 | 做法 | 价值 |
|---|---|---|
| **历史上的今天** | 打开小程序自动定位"今天 X月X日"的照片并置顶 | 强日活钩子，天然留存 |
| **社交分享** | `onShareAppMessage` 分享某天给好友/群；`onShareTimeline` 发朋友圈 | 裂变获客主通路 |
| **日签分享卡** | canvas 合成"正面图 + 日期 + 一句文案"长图 | 高传播性视觉物料 |
| **订阅消息** | 下单式申请：提醒"回看 / 新内容上线" | 唤醒回流 |
| **公众号/视频号导流** | 若已有公众号，菜单/文章嵌小程序 | 存量流量转化 |

---

## 七、性能与合规

- **性能**：缩略图 + `lazy-load` 懒加载 + CDN；`setData` 只传视图所需字段、合并调用；云数据库按月查询、分页。
- **合规**：图片为用户自有内容，注意肖像权与隐私；如涉及收集用户信息需配置隐私协议与授权弹窗；首次提审准备《隐私保护指引》与账号用途说明，避免常见驳回（无场景申请相册/位置权限等）。

---

## 八、上线路线图（快速上线）

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P0 资产预处理** | 写 node/python 脚本：解析文件名→生成三档 WebP→上传云存储→写云数据库 | 365 天数据就绪 |
| **P1 脚手架+云开发** | 建项目、配 app.json/tabBar、开通云环境、request/auth 封装 | 可运行空壳 |
| **P2 核心页** | 月历网格主页 + 翻卡详情 | MVP 可演示 |
| **P3 增强** | 年视图 + 故事流 + 分享卡 + 历史上的今天 | 完整体验 |
| **P4 提审上线** | 隐私协议、真机测试、提交审核 | 正式发布 |

---

## 九、已确认决策（2026-07-06）

1. **反面 = 另一张照片** → 翻卡背面直接显示该照片（双图翻转）
2. **存储 = 微信云开发 CloudBase** → 免服务器、最快上线
3. **首版 = 纯展示 MVP** → 月历网格 + 翻卡详情

## 十、已产出脚手架（MVP + 完整版）

代码已生成于 `miniprogram/` 目录（微信开发者工具可直接导入运行）：

**MVP**
- 前端：`app.*` + `pages/calendar`（月历网格主页）+ `pages/day-detail`（翻卡详情，正面↔反面双图）+ `utils/`
- 数据管线：`tools/preprocess.js`（生成 thumb 400 / preview 1080 WebP + manifest）+ `tools/upload.js`（上传云存储并写 `calendar_photos` 集合）

**完整版（已补齐，2026-07-06）**
- `pages/year` —— 年视图（12 个月缩略拼图，按"同一日期跨月"对角线排布，点月 `reLaunch` 跳到日历并定位该月）
- `pages/story` —— 故事流（全屏左右滑浏览某月，点击翻正/反面，可一键生成日签卡）
- 历史上的今天 —— 日历主页顶部 banner 自动定位当天照片（`getDayPhoto(today)`）
- `pages/card` + `utils/quotes.js` —— 日签分享卡（Canvas 2D 合成"图 + 日期 + 文案"，保存到相册 / 分享）
- tabBar：日历 / 年视图

**运行**
你填 AppID + 云环境 ID，按 `miniprogram/QUICKSTART.md` 跑预处理/上传脚本即可真机预览。完整版无需额外后端。

## 十一、仍可选的后续增强
- 订阅消息：提醒"回看 / 新内容上线"，唤醒回流（需服务端模板）。
- 我的页（分包）：关于、缓存清理、隐私协议入口。
- 视频号/公众号导流：若已有公众号，菜单与文章嵌小程序。
- 性能再榨：主包体积监控、云数据库索引、`calendar_photos` 按月索引查询优化。

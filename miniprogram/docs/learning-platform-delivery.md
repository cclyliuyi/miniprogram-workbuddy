> 更新：按用户反馈，已删除阻抗匹配路线及新增搜索入口框；以下是上一轮交付历史，路线验收项不再适用。辐射方向图 3D 的分包引用已修正，新增 tools/radiation-test.js 验证分包边界、实际 Three.js 几何与初始化错误处理。

# 学习平台改进与验收

## 本次交付
基础可靠性、跨模块搜索收藏、阻抗匹配路线。保留工作区原有改动；未部署云函数、未发布。

## 回归
`node tools/regression-test.js` 验证真实模块的年份、复习、缓存、离线回退、资源链接、收藏兼容、路线状态、图片重试与停留计读。

## 微信与真机验收
- 编译所有分包，确认首页新入口不遮挡固定导航，窄屏文字可读。
- 断网打开日历仍可阅读文字；联网后重试图片。云图片实际签名过期需验证。
- 详情快速滑动不计读，停留 1.5 秒计读，离开或切后台不继续计读。
- 从专题打开四个步骤，返回专题时不重复增加页面栈；勾选完成并重进后保留。
- 搜索 Agent、SMITH、驻波比，打开结果、收藏/取消，在收藏页检查同步。
- 2028 年 2 月 29 日进入复习卡，年度课程总数仍为 365。
- iOS/Android 检查 3D 前后台恢复；记录首屏时间、365 swiper 节点内存、连续切实验的帧率和内存。

## 后续以数据决定的工作
本次未改造 365 swiper，也未声称实测包体或帧率改善。后续根据真机结果决定窗口化与资源分包；其他专题、跨设备同步、计算参数对比和全内容来源校订需逐项推进。2027 以外年份的节气需要可靠多年份数据后再开启。

/* pages/tools/calc/calc.wxss —— RF 换算合集
   共享 UI 见 styles/tool-ui.wxss（tp-* 类），本文件只放页面特有覆盖。
   chips / 直觉卡 / 质量分级（tc-chip / tc-insight / tc-grade）沉淀在 app.wxss 全局。 */
@import "../../../styles/tool-ui.wxss";

/* 输入行内嵌选项按钮组（介质 / 材料选择）撑满剩余宽度 */
.tp-input-row .tp-btns { flex: 1; }

/* 质量分级徽章与输入区留一点呼吸 */
.tp-section .tc-grade { margin-top: 16rpx; }

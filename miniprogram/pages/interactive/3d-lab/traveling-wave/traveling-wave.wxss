/* 行波天线 —— 公共舞台样式取自 ../lab3d.wxss（内含 tool-ui.wxss） */
@import "../lab3d.wxss";

/* 长线模式下 V 形半角滑块置灰 */
.tw-dim { opacity: 0.4; }
.tw-slider-tip {
  margin-top: 4rpx;
  font-size: 22rpx;
  color: var(--muted);
}

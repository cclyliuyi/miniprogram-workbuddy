/* pages/interactive/aperture/aperture.wxss —— 口径衍射 */
@import "../../../styles/tool-ui.wxss";

/* 形状按钮组与下方滑杆之间留白 */
.ap-btns-gap { margin-bottom: 16rpx; }

/* 锥削滑杆在非锥削照明下的禁用态 */
.ap-row-dim { opacity: 0.4; }
.ap-hint {
  font-size: 20rpx;
  color: var(--muted);
  padding: 4rpx 0 0 176rpx;
}
